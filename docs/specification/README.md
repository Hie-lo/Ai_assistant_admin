# Project Specification Package

این پوشه قرار است میزبان ۲۲ سند رسمی طراحی پروژه باشد.

## وضعیت

پکیج کامل طراحی (۲۲ سند) در تاریخ 2026-09-12 توسط مالک تهیه شده و توسط AI Developer
مطالعه و به‌عنوان قرارداد پیاده‌سازی پذیرفته شد. انتقال نهایی فایل‌ها به این پوشه در
Phase 0 انجام می‌شود.

## ترتیب خواندن رسمی
مشترک به `PROJECT_PACKAGE_README.md` (سند ۱۸ پکیج) و بخش «ترتیب خواندن اسناد»
در README اصلی ریپو.

## نکته governance
این اسناد **قرارداد** هستند، نه مجوز برای حدس‌زدن رفتار. تا تصویب Gates در
`FINAL_APPROVAL_GATES_V1` انجام نشود، پیاده‌سازی پایه آغاز نمی‌شود.
