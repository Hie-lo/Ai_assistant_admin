# Runtime dependencies — دستیار هوشمند کسب‌وکارهای مجازی
# Stack per TECHNOLOGY_AND_REPO_SPECIFICATION_V1 (Gate F approved 2026-09-13).
# These are the resolved versions from the first compatibility run (Python 3.11).
# Final production lock will be re-verified on Python 3.13 in CI (requirements.lock.txt).

fastapi==0.141.1
uvicorn[standard]==0.52.4
sqlalchemy==2.0.52
alembic==1.20.0
pydantic==2.11.4
pydantic-settings==2.15.0
httpx==0.28.1
celery==5.6.3
redis==8.1.0
psycopg[binary]==3.3.5
openpyxl==3.1.5
jinja2==3.1.6
python-dotenv==1.2.3
google-api-python-client==2.200.0
google-auth==2.58.0
argon2-cffi==25.1.0
